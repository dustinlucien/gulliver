<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=us-ascii" />
  <meta name="keywords" content="passport photos, visa photos, passport photo instructions, US passports, travel">
  <meta name="description" content="Get your passport photos online.  Fast, flexible and guaranteed.">
  
  <title><%= title %></title>
  <%= csrf_meta_tag %>
  <%= render 'layouts/stylesheets' %>
  <%= render 'layouts/javascript' %>
  <%= yield :head %>
</head>
<body>
  <div id="container">
    <div id="wrapper" class="container">
      <%= render 'layouts/header' %>
    
      <%= yield %>
    
      <%= render 'layouts/footer' %>
    </div>
  </div>
  <%= render 'layouts/googleanalytics' %>  
</body>
</html>
